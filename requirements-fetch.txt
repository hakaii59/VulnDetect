# Heavy deps needed ONLY by the "rebuild model.onnx from a HuggingFace
# checkpoint" path (scripts/fetch_model.py without --tokenizer-only) and by
# scripts/export_onnx.py. Installing torch just to run ONNX inference would be
# wasteful, so these are installed on top of requirements-ci.txt only in the
# job that actually rebuilds the model.
torch==2.14.0
onnx==1.22.0
accelerate==1.14.0
